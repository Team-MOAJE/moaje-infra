server {
    listen 127.0.0.1:18555;
    server_name localhost;
    access_log off;
    location = /jwks.json {
        root /usr/share/nginx/html;
        default_type application/json;
    }
    location / { return 404; }
}
